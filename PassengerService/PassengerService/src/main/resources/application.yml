# Database Configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bus_passenger
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # Set to "create" for development, "update" for production
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

# Hibernate Configuration
#hibernate:
#  hbm2ddl:
#    auto: update
#  show_sql: true
#  dialect: org.hibernate.dialect.MySQL8Dialect
#  format_sql: true


server:
  port: 8083

